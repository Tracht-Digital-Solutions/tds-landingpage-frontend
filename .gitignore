node_modules/
dist/
.astro/
.env
.env.local
.DS_Store
*.log
.idea/
.vscode/
scripts/og-smoke.png

# Active local Claude worktrees live here but are not part of this checkout.
.claude/worktrees/

# Windows OS files
Thumbs.db
Desktop.ini

# Editor backups
*.swp
*~

# Test coverage
coverage/

# Cache & incremental build info
.eslintcache
*.tsbuildinfo

# Host-side setup wizard, copied out of tds-shared by the prebuild step.
# Generated, never edited here — see scripts/sync-installer.mjs.

# The deployable tree scripts/pack-release.mjs assembles from dist/.
# CI publishes it to the dev/release branch; it is never committed here.
release/

# Local page-cache store. On the host this lives outside the checkout
# (TDS_CACHE_DIR); locally it defaults to <root>/var/page-cache.
var/
