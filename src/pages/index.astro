---
/**
 * The German home page. A wrapper, like the business card routes: everything a
 * home page is lives in `components/HomePage.astro`, so the two language trees
 * cannot drift apart.
 */
import HomePage from "~/components/HomePage.astro";
---

<HomePage lang="de" />
